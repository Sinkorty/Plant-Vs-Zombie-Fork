using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// 保存进度的时候可能会需要这个类，所以就先创建了，以防万一
public class SeedPacketModel
{
    //private int bankId;
    //private float cooldownTimer;

    //public SeedPacketModel(int bankId)
    //{
    //    this.bankId = bankId;

    //    PlantSO plantSO = GameManager.Instance.GetModel().GetPlantSOFromSeedBankById(bankId);
    //}
}
